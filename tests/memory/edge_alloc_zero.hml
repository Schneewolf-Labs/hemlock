// Test allocating zero bytes
// Expected: Testing behavior (may return null or valid pointer)

let ptr = alloc(0);
if (ptr == null) {
    print("alloc(0) returned null");
} else {
    print("alloc(0) returned non-null pointer");
    free(ptr);
}

print("PASS");
